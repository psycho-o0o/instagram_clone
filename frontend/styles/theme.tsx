import { DefaultTheme } from "styled-components";

const theme : DefaultTheme = {
    colors: {
        gray: 'rgb(142, 142, 142)',
        skyBlue : 'rgb(0, 149, 246)',
    },
};

export default theme;
